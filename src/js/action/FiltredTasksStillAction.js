import { FILTRED_TASKS_STILL } from "../constants/constants";

const FiltredTasksStillAction = (payload) => {
  return {
    type:FILTRED_TASKS_STILL , payload
  }
}

export default FiltredTasksStillAction

